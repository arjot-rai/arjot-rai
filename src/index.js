import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// TODO:
// Add animation to the profile section
// Implement links for sections
// update resume
// Change the theme to make it look different - change how experience is displayed
// make colors consistent